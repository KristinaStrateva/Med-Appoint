import { Request, Response } from 'express';
import asyncHandler from 'express-async-handler';

const getAllAppointmentsForADoc = asyncHandler(async (req: Request, res: Response) => {

});

export default { getAllAppointmentsForADoc };
